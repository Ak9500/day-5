var arr = ["guvi", 'geek', 'zen', 'fullstack'];
var ano = function(arr) {
 for (var i = 0; i <= arr.length; i++) {
 console.log(arr[i][0].toUpperCase(0) + arr[i].substr(0));
 }
}
 console.log(arr)